# NetPulse

**Система для снижения социальной тревожности и организации совместной деятельности в реальном времени.**

Пользователи отображают честные статусы присутствия, добавляют друзей и запускают синхронизированные сессии (работа, учёба, медитация) с общим таймером. Приложение держит связь в реальном времени через WebSocket и REST API.

## Описание

После регистрации по уникальному имени пользователь задаёт **статус** (Онлайн, Не в сети, Работаю, Учусь и др.). В списке друзей видны актуальные статусы. Добавлять друзей можно по поиску или по **QR-коду**. Пользователям со статусом «Онлайн» можно отправлять **приглашения на сессию**; при принятии запускается общий синхронизированный таймер на вкладке «Сессия». Так участники остаются на одной «волне» без лишней переписки.

### Основные возможности

- 👤 **Регистрация и профиль** — уникальное имя, предустановленные статусы
- 👥 **Друзья** — список с актуальными статусами, поиск и добавление по имени или QR
- 📩 **Заявки в друзья** — входящие запросы, принятие или отклонение
- 📨 **Приглашения на сессию** — отправка только пользователям «Онлайн», уведомления и ответ (принять/отклонить)
- ⏱️ **Синхронизированные сессии** — общий таймер для парной или групповой активности (работа, учёба, медитация)
- 📱 **QR-код профиля** — быстрый обмен контактом для добавления в друзья

## Технологии

- **SwiftUI** — интерфейс
- **Combine** — реактивные потоки и привязка данных
- **Firebase** — аутентификация, Cloud Firestore / Realtime Database (пользователи, друзья, сессии)
- **WebSocket / REST** — реальное время и API (по архитектуре сервера)

## Архитектура

- **Clean Architecture + MVVM** — разделение слоёв и отдельные ViewModel для экранов
- **Слои:** App (точка входа), Core (сеть, конфиг), Data (модели, менеджеры, сервисы), Presentation (экраны и общие компоненты)
- **Менеджеры и сервисы** — UserManager, SessionManager, FirebaseUserService для пользователей, друзей, приглашений и сессий

## Требования

- iOS 17.0+
- Xcode 15.0+
- Язык интерфейса: русский

## Структура проекта

```
NetPulse/
├── App/                        # Точка входа (NetPulseApp)
├── Core/
│   ├── FirebaseConfig.swift   # Конфигурация Firebase
│   ├── NetworkManager.swift   # Сетевой слой
│   └── NetworkServiceProtocol.swift
├── Data/
│   ├── Models/                # User, UserStatus, FriendRequest, Invitation, Session, SessionType
│   ├── UserManager.swift      # Пользователь, друзья, заявки
│   ├── SessionManager.swift   # Сессии и приглашения
│   └── FirebaseUserService.swift
├── Presentation/
│   └── View/
│       ├── Auth/              # Регистрация, вход
│       ├── Common/            # AppTheme и общие компоненты
│       ├── Friends/           # Список друзей, заявки, добавление по поиску/QR, приглашения
│       ├── Main/              # Главный TabView, Home
│       ├── Profile/           # Профиль, статус, мой QR
│       └── Session/           # Принятие приглашения, активная сессия, таймер
├── Resources/                 # Assets.xcassets
└── GoogleService-Info.plist   # Конфигурация Firebase (не в репозитории по умолчанию)
```

## Запуск

1. Клонировать репозиторий.
2. Добавить `GoogleService-Info.plist` из консоли Firebase в корень целевой папки приложения.
3. Открыть `NetPulse.xcodeproj` в Xcode.
4. Выбрать симулятор или устройство и запустить (⌘R).

## Лицензия

Проект распространяется по лицензии MIT. Подробности — в файле [LICENSE](LICENSE).
